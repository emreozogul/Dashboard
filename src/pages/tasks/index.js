import React from "react";
import Layout from "@/components/Layout";
import { CardContainer, Card } from "@/components/TaskComponents";
const TasksPage = () => {
  return (
    <Layout>
      <CardContainer />
    </Layout>
  );
};

export default TasksPage;
